stages:
- build
- test

build:
  stage : build
  script: 
    - cd StudentHousing
    - ./gradlew build -x test
    - ./gradlew.bat --stop

test:
  stage: test
  script:
    - cd StudentHousing
    - ./gradlew test
    - ./gradlew.bat --stop